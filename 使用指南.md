# CLAUDE.MD 使用指南

> 📘 **如何使用这个文件**：当你需要添加新内容时，先查看下面的分类，找到对应的章节添加。

---

## 📂 各章节用途说明

### ✅ **Quick Context**

**用途**：项目的一句话概述，让 AI 快速理解项目性质  
**适合添加**：

- 项目核心技术栈变更
- 项目定位变化（例如从个人作品集变成企业官网）

**不适合**：详细的技术说明、配置细节

---

### ✅ **Tech Stack**

**用途**：列出项目使用的核心技术和工具  
**适合添加**：

- 新引入的重要库或框架（例如加入 Redux、React Query）
- 工具升级（例如 Vite 7 → Vite 8）
- 开发工具变更（例如从 npm 换到 pnpm）

**不适合**：临时测试的库、小工具类插件

---

### ✅ **Development Workflow**

**用途**：日常开发用到的命令和关键文件位置  
**适合添加**：

- 新的 npm scripts（例如 `pnpm test`、`pnpm storybook`）
- 新的重要文件位置（例如增加 API 层后的 `src/api/` 目录）
- 环境配置相关命令

**不适合**：一次性的临时命令、调试用的命令

---

### ✅ **Code Standards & Conventions**

**用途**：编码规范、代码风格、项目约定  
**适合添加**：

- TypeScript 使用规则（例如禁止 `any`、必须写接口）
- 文件命名规范（例如组件用 PascalCase）
- 代码组织规则（例如 hooks 必须以 `use` 开头）
- Import 顺序规范
- State 管理规则
- 注释规范

**示例**：

```markdown
### Error Handling Standards // ← 新增

- Always wrap async operations in try-catch
- Use custom Error classes for API errors
- Log errors to console in development only
```

**不适合**：具体的业务逻辑实现、UI 设计规范（那是 Design System 的内容）

---

### ✅ **Design System Reference**

**用途**：UI/UX 设计规范、视觉标准、组件样式  
**适合添加**：

- 颜色系统变更（例如增加新的主题色）
- 字体规范（例如新增中文字体）
- 间距系统调整
- 组件样式模板（例如新的卡片样式、按钮变体）
- 动画标准（例如统一的过渡时长）
- 响应式断点规则
- 无障碍设计标准

**示例**：

```markdown
### Icon System // ← 新增

- Size: w-5 h-5 (20px) for inline icons
- Size: w-8 h-8 (32px) for feature icons
- Color: text-primary for interactive, text-gray-600 for decorative
```

**不适合**：具体某个页面的布局、业务逻辑相关的样式

---

### ✅ **Common Tasks**

**用途**：重复性操作的步骤模板、日常开发工作流  
**适合添加**：

- 添加新功能的标准流程（例如"如何添加新页面"）
- 内容更新步骤（例如"如何修改个人简介"）
- 部署前检查清单
- 常见操作的快捷方式

**示例**：

````markdown
### Adding a New Page // ← 新增

1. Create component in `src/pages/PageName.tsx`
2. Add route in `src/App.tsx`:

```tsx
<Route
  path="/page-name"
  element={<PageName />}
/>
```

3. Add navigation link in `Header.tsx`
4. Update sitemap if SEO matters
````

**不适合**：一次性的特殊任务、不会重复的操作

---

### ✅ **Project Context for AI Assistance**

**用途**：告诉 AI 你的优先级、约束条件、审美偏好  
**适合添加**：

- 项目的设计原则（例如"简洁优先于花哨"）
- 技术选型偏好（例如"优先使用原生方案而非库"）
- 性能/可访问性优先级
- 代码审查标准
- 禁止事项（例如"不使用 jQuery"）

**示例**：

```markdown
### Design Preferences: // ← 新增

- Minimalism over complexity
- Subtle animations (avoid flashy effects)
- Prefer custom CSS over heavy animation libraries
- Mobile experience is as important as desktop
```

**不适合**：具体的技术实现细节

---

### ✅ **Deployment**

**用途**：部署相关的配置和流程  
**适合添加**：

- 部署平台配置（例如 Vercel、Netlify 设置）
- 环境变量说明
- 部署前检查清单
- CI/CD 配置说明
- 域名和 DNS 配置

**示例**：

```markdown
### Staging Environment // ← 新增

- URL: https://staging.jasonguo.dev
- Branch: `develop`
- Auto-deploys on push to develop
- Password protected: check 1Password
```

**不适合**：敏感信息（密码、API keys）要用环境变量

---

### ✅ **Troubleshooting** ⭐ 新章节

**用途**：常见问题和解决方案、bug 修复记录  
**适合添加**：

- 反复出现的错误和解决方法
- 环境相关的问题（例如 Node 版本不兼容）
- 依赖冲突的解决方案
- 平台特定的问题（例如 Vercel 部署失败）

**示例**：

````markdown
## Troubleshooting

### Build Fails on Vercel

**Error**: "Cannot find module 'framer-motion'"
**Solution**: Add to `package.json` devDependencies:

```bash
pnpm add -D framer-motion
```

### Slow Initial Load

**Symptom**: First load takes 5+ seconds
**Solution**: Check for large unoptimized images in `public/`
````

**不适合**：临时的、已修复的、不会再出现的问题

---

### ✅ **Performance Optimization** ⭐ 新章节

**用途**：性能指标、优化技巧、性能测试结果  
**适合添加**：

- Lighthouse 分数记录
- Bundle size 追踪
- 性能优化技巧和最佳实践
- 加载速度优化方案
- 性能测试工具配置

**示例**：

```markdown
## Performance Optimization

### Current Metrics (2026-01-07)

- Lighthouse Performance: 96/100
- FCP: 1.2s
- LCP: 1.8s
- Bundle Size: 152KB gzipped

### Recent Optimizations

- [2026-01-05] Lazy loaded Portfolio component: -30KB
- [2026-01-03] Optimized images with WebP: -150KB
- [2025-12-20] Removed unused Tailwind classes: -12KB
```

**不适合**：临时的性能测试、未验证的优化建议

---

### ✅ **Future Enhancements** ⭐ 新章节

**用途**：未来计划、待办事项、技术债务  
**适合添加**：

- 计划中的新功能
- 想要尝试的新技术
- 已知的技术债务
- 改进想法和实验性功能

**示例**：

```markdown
## Future Enhancements

### High Priority

- [ ] Add blog section with MDX support
- [ ] Implement contact form backend
- [ ] Add Google Analytics

### Nice to Have

- [ ] Dark mode auto-switch based on time
- [ ] Custom cursor for desktop
- [ ] Easter egg: Konami code animation

### Technical Debt

- [ ] Refactor Portfolio component (too large)
- [ ] Add unit tests with Vitest
- [ ] Set up Storybook for component docs
```

**不适合**：已完成的功能、已放弃的想法

---

## 🎯 快速决策流程图

```
收到新内容要添加
    ↓
问自己：这是什么类型的内容？
    ↓
┌─────────────────────────────────────────────────┐
│ 编码规范/命名约定？ → Code Standards           │
│ UI样式/颜色/间距？  → Design System            │
│ 重复性操作步骤？    → Common Tasks              │
│ AI 辅助的规则？     → Project Context for AI   │
│ 部署相关配置？      → Deployment                │
│ 常见问题/错误？     → Troubleshooting ⭐        │
│ 性能优化相关？      → Performance Optimization ⭐│
│ 未来计划/待办？     → Future Enhancements ⭐    │
└─────────────────────────────────────────────────┘
```

---

## 📝 实际示例演练

### 场景 1：你想添加"所有 API 调用必须有超时处理"

**应该添加到**：`Code Standards & Conventions`  
**理由**：这是编码规范

````markdown
### API Call Standards // ← 在 Code Standards 章节新增

- All API calls must have timeout (default: 10s)
- Use axios with timeout config:

```typescript
const api = axios.create({
  timeout: 10000,
  headers: { 'Content-Type': 'application/json' },
});
```
````

---

### 场景 2：你想添加"按钮的悬停效果标准"

**应该添加到**：`Design System Reference`  
**理由**：这是 UI 设计规范

```markdown
### Button Hover Effects // ← 在 Design System 章节新增

- Duration: 200ms
- Lift effect: `hover:-translate-y-1`
- Shadow: `hover:shadow-xl`
- Color brightness: `hover:brightness-110`
```

---

### 场景 3：你想添加"如何修改社交媒体链接"

**应该添加到**：`Common Tasks`  
**理由**：这是重复性操作

````markdown
### Updating Social Links // ← 在 Common Tasks 章节新增

1. Edit `src/data/portfolio.ts`
2. Find `contactInfo.socials` object
3. Update URLs:

```typescript
socials: {
  github: 'https://github.com/new-username',
  linkedin: 'https://linkedin.com/in/new-username'
}
```

4. Check Footer.tsx for display
````

---

### 场景 4：你想添加"Vercel 部署时偶尔失败的解决方法"

**应该添加到**：`Troubleshooting`（新章节）  
**理由**：这是常见问题修复

```markdown
## Troubleshooting // ← 创建新章节

### Vercel Build Fails Randomly

**Error**: "Process exited with code 137"
**Cause**: Out of memory during build
**Solution**:

1. Go to Vercel project settings
2. Change Node.js version to 18.x
3. Add environment variable: `NODE_OPTIONS=--max_old_space_size=4096`
```

---

### 场景 5：你想记录"未来要加博客功能"

**应该添加到**：`Future Enhancements`（新章节）  
**理由**：这是未来计划

```markdown
## Future Enhancements // ← 创建新章节

### Content Features

- [ ] **Blog section** - MDX for technical writing
  - Use `@next/mdx` or `remark`
  - Code syntax highlighting with Prism
  - Reading time estimate
- [ ] **Project case studies** - Detailed write-ups
```

---

## 💡 维护建议

### 每次添加内容后：

1. **检查重复**：同类信息是否已存在？可以合并吗？
2. **保持简洁**：能用一句话说清楚吗？代码示例必要吗？
3. **更新日期**：改了重要内容记得更新文件底部的 "Last Updated"
4. **测试可读性**：让 Claude 读一遍，看能否快速理解

### 定期清理（每月一次）：

- 删除过时的内容（例如已弃用的技术）
- 合并相似的章节
- 更新性能指标
- 检查外部链接是否失效

---

## 🚫 反面教材：不要这样做

### ❌ 错误示例 1：信息放错位置

```markdown
## Code Standards & Conventions

### Button Colors // ← 错误！这应该在 Design System

- Primary: #3B82F6
- Secondary: #8B5CF6
```

### ❌ 错误示例 2：过于啰嗦

```markdown
## Common Tasks

### Adding New Project

First, you need to navigate to the src folder, then find the data folder inside it, and then open the portfolio.ts file. After that, you need to scroll down until you find the projects array... // ← 太长了！
```

**正确做法**：

````markdown
### Adding New Project

Edit `src/data/portfolio.ts` projects array:

```typescript
{ title: 'Project', description: '...', technologies: [...] }
```
````

### ❌ 错误示例 3：没有更新日期

```markdown
---

**Last Updated**: January 2026 // ← 已经改了很多次但没更新日期
```

---

## ✅ 总结：记住这个口诀

```
规范看 Standards（编码规范）
样式看 Design（UI 设计）
操作看 Tasks（日常任务）
AI 看 Context（辅助规则）
部署看 Deployment（上线配置）
问题看 Troubleshooting（排错）
性能看 Optimization（优化）
未来看 Enhancements（计划）
```

---

**记住**：这个文件是给 AI 和未来的你看的，保持**简洁、有序、易查找**！
